DATABASE_URL="postgresql://postgres:change-me@localhost:5432/foodflow?schema=public"
JWT_SECRET="replace-with-a-long-random-secret"
CLIENT_ORIGIN="http://localhost:5173"
# Separate multiple allowed frontend URLs with commas when needed.
PORT=4000
